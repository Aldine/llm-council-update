# OpenRouter API Key
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# JWT Secret Key (for mobile app authentication)
# Generate with: openssl rand -hex 32
# Or use online generator: https://generate-secret.vercel.app/32
JWT_SECRET_KEY=dev-secret-key-change-in-production

# =============================================================================
# BFF OAuth Configuration (for web app with server-side sessions)
# =============================================================================

# Use mock OAuth for local development (true) or real provider (false)
OAUTH_USE_MOCK=true

# Real OAuth Provider Settings (when OAUTH_USE_MOCK=false)
# Register your OAuth app with provider (Auth0, Okta, Google, Azure AD, etc.)
# See OAUTH_SETUP.md for detailed instructions

OAUTH_ISSUER=https://your-domain.auth0.com
OAUTH_AUTHORIZATION_ENDPOINT=https://your-domain.auth0.com/authorize
OAUTH_TOKEN_ENDPOINT=https://your-domain.auth0.com/oauth/token
OAUTH_USERINFO_ENDPOINT=https://your-domain.auth0.com/userinfo
OAUTH_REVOCATION_ENDPOINT=https://your-domain.auth0.com/oauth/revoke
OAUTH_CLIENT_ID=your_client_id
OAUTH_CLIENT_SECRET=your_client_secret
OAUTH_SCOPES=openid profile email

# OAuth Redirect URI (where provider sends user after login)
# Local dev: http://localhost:8001/bff/auth/callback
# Production: https://api.yourdomain.com/bff/auth/callback
OAUTH_REDIRECT_URI=http://localhost:8001/bff/auth/callback

# Frontend URL (where to redirect after successful login)
# Local dev: http://localhost:5173
# Production: https://app.yourdomain.com
APP_BASE_URL=http://localhost:5173

# Session Security
# Generate with: openssl rand -hex 32
SESSION_SECRET=dev-session-secret-change-in-production

# Cookie Settings
COOKIE_SECURE=false  # Set to true in production (HTTPS required)

